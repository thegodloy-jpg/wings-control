# wings-control 依赖清单

# Web 框架
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
pydantic>=2.9.0,<3.0
pydantic-settings>=2.5.0,<3.0

# HTTP 客户端
httpx>=0.27.0,<1.0
requests>=2.32.0

# 序列化
orjson>=3.9.0
PyYAML>=6.0

# 配置
python-dotenv==1.0.0

# 以下包仅在引擎容器中使用，sidecar 不需要安装：
# ray, torch, pynvml, torch-npu