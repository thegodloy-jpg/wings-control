# 需求一 · 三特性使能改造 — 完成度核查与遗漏清单

> 对照基线：[需求一-三特性使能.md](需求一-三特性使能.md)。
> 核查方式：以工作树**实际代码状态**逐项验证（非仅看提交信息），结论标注 file:line 可溯源。
> 初次核查：2026-06-26。涉及提交：`4bed278`/`4224e0f`/`f95737f`/`73b177b`/`d3eb3ee` + 更早的 C4/variants 落地。
>
> **更新（2026-06-27 本轮落地）**：点 6（SPARSE_LEVEL）已实现、点 1（fp8/算子加速残留）已完全删除、点 3（特性日志）已按建议增强。对应 G1/G5 闭环。详见文末「五、本轮落地记录」。

---

## 一、八点完成度总览

| # | 诉求点 | 状态 | 落点 |
| --- | --- | --- | --- |
| 1 | 删除 fp4 / fp8 | ✅ 完成（本轮闭环） | 自动量化注入链路全删；两条引擎路由旁路 + `use_kunlun_atb` 死代码**已清除**（§6-⑤ 落地，有一处引擎选择行为变化） |
| 2 | 对外暴露特性状态接口 | ✅ 完成 | `/v1/startup/accel` 读 `ADVANCED_FEATURES_FILE`，每特性透出 name/enabled/variant/errMsg |
| 3 | 对内特性日志 | ✅ 完成（本轮增强） | variants 落 json + logger；新增卡型 miss 告警 + 收口 req→eff 摘要 + sparse 抑制对称日志 |
| 4 | 三特性依赖白名单 | ✅ 完成 | `SMART_FEATURE_WHITELIST` + `apply_effective_feature_enablement` 收口 + PD 一票否决 |
| 5 | 内存自动计算（KV 卸载 auto 容量） | ✅ 完成（系数待运维校准） | `resolve_offload_cpu_capacity_gb` C4 + 强制 swap_space=0 + 熔断 100G |
| 6 | 稀疏多模式判定（精度/性能优先） | ✅ 完成（本轮落地） | `get_sparse_level_env` + `_resolve_sparse_level`；performance_first 生效，topk 由 sparse 表 per-row 配置决定 |
| 7 | 硬件信息保留老逻辑 / 依赖 ENGINE-VERSION | ✅ 完成 | `resolve_card_token` 兜底链含 engine-version 后缀（a2/a3→910b/910c） |
| 8 | 打补丁逻辑保持现状 | ✅ 保持现状 | `_collect_indexcache_patch_features` engine 门控只接 vllm；ascend day0 走 `--hf-overrides` 不打补丁 |

---

## 二、逐点详解

### 点 1 · 删 fp4/fp8 — ✅ 已完全删除（本轮闭环）

- **已删**（`73b177b`）：`_set_soft_fp8`/`_set_soft_fp4` 及 helper、`get_soft_fp4_env`、`_build_deepseek_fp8_env_commands`、官方 W8A8 路径 `_set_deepseek_v3_family_ascend_quant_params` / `_is_deepseek_v3_family_model`、model_utils 一众探测器（`is_qwen3_series_fp8` 等）。
- **本轮再删（§6-⑤ 落地，残留清零）**：
  - `_select_ascend_engine` 的两条引擎路由旁路：`ENABLE_OPERATOR_ACCELERATION` → vllm_ascend、`ENABLE_SOFT_FP8` → vllm_ascend；
  - env_utils 的 `get_operator_acceleration_env` / `get_soft_fp8_env` 及 config_loader 对应 import；
  - `use_kunlun_atb` 死代码全链：`_set_task` 的两处置 False、`_build_vllm_ascend_extensions` 空壳函数 + 调用点、`_strip_internal_engine_config_keys` 的 pop；
  - 同步 `test_unit_engine_select.py`（删两条分支测试 + patch，分支数 7→5）。
- **唯一行为变化（已与用户确认）**：「Wings 已验证模型 + `ENABLE_OPERATOR_ACCELERATION`/`ENABLE_SOFT_FP8`=true」→ 引擎自动选择从 vllm_ascend 变为 mindie。符合 §6-⑤「暂按可删」。
- **残留**：仅文档 `wings_control/docs/test-infer-decouple.md` T-3.8 行仍描述已不存在的 `use_kunlun_atb` CLI 过滤（可选清理）。**对应评审补充问题一，已解决**。

### 点 2 · 对外接口 — 完成（评审补充问题二已被修复）

- 评审补充问题二里贴的 `accel_file = settings.ACCEL_FILE` + `_read_accel_features` 是**旧代码**。当前 [health_service.py:298-319](../../wings_control/proxy/health_service.py#L298) 已改读 `settings.ADVANCED_FEATURES_FILE`，单文件单一真相源，路径互串 bug 已在 `f95737f` 修掉。**问题二无需再处理**。

### 点 3 · 对内日志 — ✅ 已增强（本轮落地，评审补充问题三）

- 原状：variants 已落 advanced_features.json + [wings_entry.py:991](../../wings_control/core/wings_entry.py#L991) 汇总日志；产出口有 `[KVCache Offload]`/`[IndexCache]`/`[EARS]`/`[SpecDecode]` 散点日志。
- 本轮按核查建议补三处可观测性（均落在 `apply_effective_feature_enablement`）：
  - 🔴 **卡型解析失败告警**：`engine=vllm_ascend` 且 `card_token` 空 → `WARNING`，提示「910b/910c 白名单将全 miss，检查 details[0].name / ENGINE_VERSION 后缀」（救 §C.5 最硬静默失败）；
  - 🟡 **收口 req→eff 摘要**：`[SmartFeature] effective enablement` 行改为一行打全 `sparse req→eff / offload req→eff / spec req + whitelist_spec`，一眼看清「请求 vs 有效」；
  - 🟡 **sparse 抑制对称日志**：用户请求 sparse 但白名单外时补一行 `suppressed`，与既有 offload 抑制日志对称。
- 未做（噪音，🟢 低价值）：`feats=[]` 正向确认、spec 单行 variant 汇总。**评审补充问题三已解决**。

### 点 4 · 三特性依赖白名单 — 完成

- [SMART_FEATURE_WHITELIST](../../wings_control/utils/model_utils.py#L53)（16 行 4-tuple）+ [resolve_feature_whitelist](../../wings_control/utils/model_utils.py#L78) / [feature_allowed](../../wings_control/utils/model_utils.py#L97)；
- [apply_effective_feature_enablement](../../wings_control/core/config_loader.py#L1808) 在 [config_loader.py:2606](../../wings_control/core/config_loader.py#L2606) 收口，PD 一票否决齐备。

### 点 5 · 内存自动计算 — 完成，系数待实测

- [resolve_offload_cpu_capacity_gb](../../wings_control/engines/vllm_adapter.py#L2215)：`M_offload = M_container − (7G×TP×DP+3G) − 10%margin`，`_OFFLOAD_MIN_GB=100` 熔断；
- [_apply_auto_offload_swap_space](../../wings_control/engines/vllm_adapter.py#L1902) 原子绑定 `swap_space=0`；
- LMCache 写回 `LMCACHE_MAX_LOCAL_CPU_SIZE = M_offload ÷ N_card`（[vllm_adapter.py:709](../../wings_control/engines/vllm_adapter.py#L709)），native 整节点不除卡数（[2264](../../wings_control/engines/vllm_adapter.py#L2264)）。
- 唯系数（7G / 3G / margin / 下限）属文档 §3.0「待运维校准」，逻辑已完整、保守兜底不阻塞主线。

### 点 6 · 稀疏多模式判定 — ✅ 已实现（本轮落地）

- env_utils 新增 `get_sparse_level_env()`：读 `SPARSE_LEVEL`，取值 `accuracy_first`/`performance_first`，缺省/非法回落 `accuracy_first`。
- vllm_adapter 新增 `_resolve_sparse_level()`：`performance_first` 原样生效；`_build_kv_sparse_cmd` 按 sparse 表 per-row topk 切换，未声明 performance 时回退本行 accuracy。
- 当前 `accuracy_first` 保持原有 topk；`performance_first` 已在 V4-Flash·NV 路径产出 topk8，未声明 performance topk 的行保持原有输出。
- 补单测 `test_unit_kv_sparse.py::TestSparseLevel`（缺省/非法/accuracy 透传/performance 透传、V4-Flash topk4→8、缺失 performance 回退 accuracy）。

### 点 7 · 硬件信息保留老逻辑 — 完成

- [resolve_card_token](../../wings_control/utils/device_utils.py#L286) 兜底链 `details[0].name → WINGS_DEVICE_NAME → engine-version 后缀 → 显存`，与文档 §1.2「依旧保留现状采用 ENGINE-version 判定，后续放需求二/三」一致。

### 点 8 · 打补丁逻辑 — 保持现状

- [_collect_indexcache_patch_features](../../wings_control/core/wings_entry.py#L458) engine 门控只接 `("vllm",)` → 0430 的 NV 模型经 `install.py --features` 装补丁；vllm_ascend（day0）天然不装、仅 `--hf-overrides` 启用 IndexCache。符合「0430 打补丁、day0 强制开/不需补丁，保持现状」。

---

## 三、其它遗漏（文档 ↔ 代码漂移 + 功能缺口）

> 上面 8 点之外，对照需求文档的 `🆕`/规划项逐条扫描，发现以下问题；**多为文档滞后于代码**，建议回写需求文档时一并修正。

| 编号 | 类型 | 位置 | 问题 | 状态 / 建议 |
| --- | --- | --- | --- | --- |
| G1 | **功能缺口** | 代码（点 6） | `SPARSE_LEVEL` 多模式判定与 performance 产出路径 | ✅ **已闭环**：`get_sparse_level_env` + `_resolve_sparse_level`，performance_first 生效并按 sparse 表 topk 产出 |
| G2 | **文档滞后** | 需求文档 §1.1 | GLM-5.2/Qwen3.6·Ascend 白名单写 `{spec,offload}`，代码已收窄为 `{spec}` | ⏳ 待办：§1.1 两行改 `{spec}`，对齐 [model_utils.py:71-72](../../wings_control/utils/model_utils.py#L71) 与 [各平台映射](../各平台加速特性与底层启动参数映射.md) |
| G3 | **文档滞后** | 需求文档 §0.1#3 / 附录 D.2 / D.3 | 称「V4-Flash·NV sparse/offload 白名单无此行 → 不可触发 / 需 §6-④」，但代码已补行 `{spec,sparse,offload}`（§6-④ 已落地） | ⏳ 待办：删去「不可触发/需§6-④」表述，标 §6-④ 已闭环 |
| G4 | **文档滞后** | 需求文档 附录 B.1 全枚举矩阵 | 仍是旧「白名单 9 行」，未含 glm-5.2 / qwen3.6 / v4-flash·NV / deepseek-v3.1 / v4-pro 等（代码已 16 行） | ⏳ 待办：重刷 B.1 矩阵为全 16 行 |
| G5 | **悬置决策** | §6-⑤ | `USE_KUNLUN_ATB` / `ENABLE_SOFT_FP8` 引擎路由旁路是否删（点 1 残留） | ✅ **已决策并删除（2026-06-27）**：两旁路 + 死代码全删；行为变化＝已验证模型+该开关→mindie（见点 1） |
| G6 | **悬置决策** | §6-① | NV 卡码（NRP0500/NH02）是否进白名单 key，现统一用 `"*"` | ⏳ 待定：需确认 NV 是否需按卡型细分特性 |
| G7 | **待运维** | §3.0 待运维表 | C4 线性系数（7G×TP×DP+3G / margin 10% / 下限 100G）尚未实测校准 | ⏳ 逻辑已落地，保守兜底先行，待实测调参 |
| G8 | **文档内口径不统一**（次要） | §1.1 line 141 vs §5 | §1.1 注称产出口复用 stash `_smart_feats`；代码实际走 §5 的「env 兜底双入口」`resolve_card_token()`（[vllm_adapter.py:2755](../../wings_control/engines/vllm_adapter.py#L2755)） | ⏳ 待办：§5 为权威口径，删/改 §1.1 line 141 的 stash 描述 |
| G9 | **既有测试失败**（与本需求无关） | `test_function_call_support` / `test_reasoning_parser_support` / `test_unit_tcp` | 6 个失败：`_LLM_MODELS`(model_utils) 与支持矩阵 yaml 漂移（如 `KimiK25ForConditionalGeneration`） | ⏳ 既有问题，非本需求引入；需另开一轮同步 yaml ↔ model_utils 清单 |

---

## 四、结论：还差什么

**功能缺口**：已清零。点 6（SPARSE_LEVEL）本轮落地，G1 闭环。

**已决策收尾**（本轮）：

- **点 1 / G5**（§6-⑤）：引擎路由旁路 + `use_kunlun_atb` 死代码**已全删**；唯一行为变化＝「已验证模型 + `ENABLE_OPERATOR_ACCELERATION`/`ENABLE_SOFT_FP8`=true → mindie」。
- **点 3**：卡型 miss 告警 + 收口 req→eff 摘要 + sparse 抑制对称日志**已补**。

**仍待决策**（非 bug）：**G6**（§6-① NV 卡码细分）。

**仅需回写需求文档、不动代码**：**G2 / G3 / G4 / G8**（[需求一-三特性使能.md](需求一-三特性使能.md) 滞后于已落地代码）。

**逻辑已完成、待外部校准**：**G7**（C4 系数）。

**与本需求无关的既有问题**：**G9**（model_utils ↔ 支持矩阵 yaml 漂移致 6 个测试 fail）。

> 一句话：**八点全部落地，功能缺口清零**；剩下的是 NV 卡码细分待定（G6）、需求文档自身的滞后描述待回写（G2/G3/G4/G8）、C4 系数待实测（G7），以及一个与本需求无关的既有测试漂移（G9）。

---

## 五、本轮落地记录（2026-06-27）

> 改动文件：`env_utils.py` / `vllm_adapter.py` / `config_loader.py` / `test_unit_kv_sparse.py` / `test_unit_engine_select.py`。
> 验证：定向用例全过（engine-select + kv-sparse + 收口/spec/v4flash/dp 共 148 passed）；G9 的 6 个失败为既有 yaml 漂移、与本轮无关。

**点 6 · SPARSE_LEVEL（新增）**
- `env_utils.get_sparse_level_env()` + 常量 `SPARSE_LEVEL_ACCURACY_FIRST` / `SPARSE_LEVEL_PERFORMANCE_FIRST`。
- `vllm_adapter._resolve_sparse_level()`：performance_first 原样生效；挂 `_build_kv_sparse_cmd` 入口并按 sparse 表 topk 产出。
- `test_unit_kv_sparse.py::TestSparseLevel`（5 用例）。

**点 1 · fp8/算子加速残留（删除）**
- `config_loader._select_ascend_engine`：删 `ENABLE_OPERATOR_ACCELERATION` / `ENABLE_SOFT_FP8` 两条路由旁路 + 文档优先级表（7→5 条）。
- `config_loader._set_task`：删两处 `use_kunlun_atb=False`（保留 `enforce_eager=True`）。
- `config_loader` import：删 `get_soft_fp8_env` / `get_operator_acceleration_env`。
- `env_utils`：删 `get_operator_acceleration_env` / `get_soft_fp8_env`。
- `vllm_adapter`：删空壳 `_build_vllm_ascend_extensions` + 调用点；删 `_strip_internal_engine_config_keys` 的 `use_kunlun_atb` pop + 注释。
- `test_unit_engine_select.py`：删两条分支测试 + 对应 patch。
- 注：`_build_base_env_commands(params,...)` 的 `params` 形参未用但保留（6 处调用接口稳定，仅 Hint）。

**点 3 · 特性日志（增强，均在 `apply_effective_feature_enablement`）**
- 🔴 `engine=vllm_ascend` 且 `card_token` 空 → `WARNING`（白名单将全 miss）。
- 🟡 `[SmartFeature] effective enablement` 摘要改一行打全 `sparse/offload/spec` 的 req→eff + whitelist_spec。
- 🟡 sparse 请求但白名单外 → 补 `suppressed` 日志（与 offload 抑制对称）。

