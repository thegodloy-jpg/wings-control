# 需求一 · smart_feature_whitelist 完备性验证报告

> 目标文件：`wings_control/config/smart_feature_whitelist.json`
> 基线：`master=0a8b3f9` 为白名单生成前状态，master 不存在该 JSON；旧能力由产出口散点逻辑决定。
> 当前分支：`feat/smart-three-feature-enablement`，验证日期：2026-07-02。

## 结论

已对 `smart_feature_whitelist.json` 做专项审计，结论为通过：

- 唯一白名单组合：12 个。
- 拆表行数：`spec=10`、`sparse=7`、`offload=7`。
- JSON 精确矩阵：PASS，0 issue。
- resolver 别名/卡型/负例：PASS，0 issue。
- 生产 dry-run variants：PASS，0 issue。
- env 绕过负例：PASS，0 issue。

本轮修正 1 个准确性问题：`Qwen3.6` Ascend spec 白名单按源表收窄为 `910c`，不再覆盖 `910b`。这不是缺失修复，而是防止白名单过宽。

本轮额外加固 1 个防绕过点：`_resolve_lmcache_install_target()` 不再只依赖已被 C14 收口后的 `ENABLE_KV_OFFLOAD`，会复用 `_smart_feats` 或白名单二次判定，避免中间层重新注入 env 后触发 LMCache 补丁安装。

## 验证器

新增脚本：

```powershell
python .\tests\verify_smart_feature_whitelist.py
```

输出：

```text
Expected unique tuples: 12
Expected split rows: spec=10 sparse=7 offload=7
[PASS] JSON schema and exact matrix: 0 issue(s)
[PASS] Resolver aliases/cards/negative cases: 0 issue(s)
[PASS] Production dry-run variants: 0 issue(s)
[PASS] Env bypass negative cases: 0 issue(s)
RESULT: PASS  issues=0
```

输出文件：`tests/smart_feature_whitelist_audit_output.txt`。

## 覆盖内容

1. JSON 结构与精确矩阵：
   - 检查三张表必须存在：`spec`、`sparse`、`offload`。
   - 检查每行 `engine/name_tokens/card_tokens/source/topk`。
   - 检查没有缺行、额外行、错误 feature 行。

2. resolver 正负例：
   - 所有别名均校验，例如 `qwen3.5-397b/qwen3_5-397b`、`glm-5.1/glm5.1`、`deepseek-v3.2/deepseek_v3.2`。
   - 卡型边界校验：`DeepSeek-V3.2` 仅 910C；`Qwen3.6` 仅 910C；Ascend 空 card 必 miss。
   - 非目标模型校验：`glm-5` 基座、`DeepSeek-V4-Pro` 不误命中。

3. 生产 dry-run：
   - 对 12 个唯一白名单组合全部跑真实下发链路。
   - 三特性变量全量经 env 下发。
   - 校验最终 `advanced_features.json` 的 `features` 和 `variants`。

4. env 绕过负例：
   - 模拟用户同时下发 `ENABLE_SPECULATIVE_DECODE/SD_ENABLE`、`ENABLE_SPARSE/SPARSE_ENABLE`、`ENABLE_KV_OFFLOAD/LMCACHE_OFFLOAD`、CPU/Disk/QAT 子开关。
   - 校验非白名单模型不能产 sparse/offload 启动参数、LMCache env、LMCache YAML、LMCache 补丁安装片段。
   - 校验 `Qwen3.6·Ascend 910B` 不再因 env 命中 MTP，仅保留允许的 suffix 地板。
   - 校验 PD 角色一票否决后，三特性均不因 env 复活。

## 当前矩阵摘要

| engine | 模型 | card | features |
| --- | --- | --- | --- |
| vllm | Qwen3.5-397B | * | spec, sparse |
| vllm | GLM-4.7 | * | spec, sparse, offload |
| vllm | GLM-5.1 | * | sparse |
| vllm | MiniMax-M2.7 | * | spec, sparse, offload |
| vllm | DeepSeek-V4-Flash | * | spec, sparse, offload |
| vllm_ascend | GLM-4.7 | 910B/910C | spec, offload |
| vllm_ascend | MiniMax-M2.5 | 910B/910C | spec, offload |
| vllm_ascend | DeepSeek-V3.2 | 910C | spec, offload |
| vllm_ascend | Qwen3.6 | 910C | spec |
| vllm_ascend | DeepSeek-V4-Flash | 910B/910C | spec, sparse, offload |
| vllm_ascend | GLM-5.1 | 910B/910C | sparse |
| vllm_ascend | GLM-5.2 | 910B/910C | spec |

## 特别说明

- `DeepSeek-V4-Flash·Ascend` sparse 白名单保留；当前产出口 variant 是 `noop`，与需求文档“630 后切 use_index_cache”一致，不按缺失处理。
- `Qwen3.6·Ascend` 按源表仅覆盖 910C；新增负例防止 910B 误命中。
- master 没有该 JSON，因此“与 master 对比”通过旧产物差异验证；“白名单本体完备性”由本专项脚本保证。
